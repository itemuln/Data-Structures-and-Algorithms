#ifndef MAX_CONSECUTIVE_ONES_H
#define MAX_CONSECUTIVE_ONES_H

/**
 * Finds the maximum number of consecutive 1's in a binary array
 * @param nums Array of integers (0s and 1s)
 * @param numsSize Size of the array
 * @return Maximum count of consecutive ones
 */
int findMaxConsecutiveOnes(int *nums, int numsSize);

#endif // MAX_CONSECUTIVE_ONES_H
